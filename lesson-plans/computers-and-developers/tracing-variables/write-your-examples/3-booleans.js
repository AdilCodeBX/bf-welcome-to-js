'use strict';

debugger;
/* Booleans

TODO: create a few variables(boolean):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// DECLARE a variable named `whereAreyou`
// INITIALIZE the value to "hello"
let whereAreyou = true;

// ADD conditions
// READ the value stored in `whereAreyou`
if (whereAreyou) {
  console.log('in Brussels');
} else {
  console.log('Sorry i think you are lost');
}
