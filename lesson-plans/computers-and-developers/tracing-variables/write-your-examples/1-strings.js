/* eslint-disable no-unused-vars */
'use strict';

debugger;

/* Strings

TODO: create a few variables(strings):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/
// DECLARE a variable named `name`
// INITIALIZE the value to `Jessica`
let artist = 'Picasso';

// READ de value store in `artist`
console.log(artist);

// ASSIGN a new value to `artist`
artist = 'Munch';

// READ de value store in `artist`
console.log(artist);
